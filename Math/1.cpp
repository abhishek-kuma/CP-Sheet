#include <bits/stdc++.h>

// Simple implementation is done just take the range b c c will be the traingle

#define fastio                        \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL)

#define endl "\n"

using namespace std;
void solve(){
    int a,b,c,d;
    cin>>a>>b>>c>>d;
    cout<<b<<" "<<c<<" "<<c<<endl;
    return;
}

int main()
{
    fastio;
    int n;
    cin>>n;
    while(n--) solve();
    return 0;
}